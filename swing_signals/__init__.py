"""swing-signals: a signal-only swing-trading signal generator for US stocks.

Decision support only. This package never connects to a broker, never places
orders, and has no auto-execution path — by design.
"""

__version__ = "0.1.0"
