# Single source of truth is the repo-root requirements.txt (Streamlit Cloud reads
# either location depending on the configured entrypoint; this pointer keeps the
# two from drifting apart — edit ONLY the root file).
-r ../requirements.txt
